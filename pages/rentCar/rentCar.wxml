<!--logs.wxml-->
<view class="map_container">
  <map id="myMap" longitude="{{lon}}" latitude="{{lat}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" show-location></map>
</view>
